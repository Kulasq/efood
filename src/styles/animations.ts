export const fadeIn = `
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
`

export const slideIn = `
  @keyframes slideIn {
    from { transform: translateY(100%); }
    to { transform: translateY(0); }
  }
`